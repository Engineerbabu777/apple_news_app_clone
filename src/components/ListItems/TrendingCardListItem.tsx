import { View, Text } from "react-native";
import React from "react";

type Props = {};

const TrendingCardListItem = (props: Props) => {
  return (
    <View>
      <Text>TrendingCardListItem</Text>
    </View>
  );
};

export default TrendingCardListItem;
